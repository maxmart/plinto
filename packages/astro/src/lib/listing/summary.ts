/**
 * Re-exported from @plinto/admin, which owns the MDX conversion this is built
 * on. Published as `@plinto/astro/lib/listing/summary` so a site's block keeps
 * importing one path.
 */
export { derivePageSummary } from '@plinto/admin/listing/summary';
export type { PageSummary, SummaryOptions } from '@plinto/admin/listing/summary';
