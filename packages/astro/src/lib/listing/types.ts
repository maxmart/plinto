/**
 * Re-exported from @plinto/admin, which owns the listing walk. The build-time
 * readers here and the site-facing hooks both speak these shapes.
 */
export type { ContentFile, CollectionEntry, PageEntry } from '@plinto/admin/listing/types';
export { applyCollectionFilters } from '@plinto/admin/listing/types';
