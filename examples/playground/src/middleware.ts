// Required by Astro for i18n.routing 'manual' — must be the site's own file.
// The passthrough itself (and the reasoning) lives in the library.
export { onRequest } from '@plinto/astro/middleware';
